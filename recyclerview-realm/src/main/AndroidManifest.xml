<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.edgon.recyclerview_realm"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activitys.DetailActivity">
        </activity>
        <activity
            android:name=".activitys.RefereciaActivity"
            android:label="@string/title_activity_referecia"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".activitys.RegisterActivity">
        </activity>
    </application>

</manifest>